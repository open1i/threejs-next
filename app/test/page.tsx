import ClientWrapper from '@/components/client-wrapper'

export default function TestPage() {
  return <ClientWrapper />
}

// 设置页面元数据
export const metadata = {
  title: '3D Model Viewer',
  description: 'A test page for viewing 3D OBJ models'
} 